<% if user_signed_in? %>
  <% if policy(HealthCareProfessional).index? %>
    <li class='nav-item'><%= link_to 'Health Care Professionals', health_care_professionals_path, class: 'nav-link' %></li>
  <% end %>
<% end %>