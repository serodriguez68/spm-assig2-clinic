<% if resource.errors.any? %>
  <div class="alert alert-danger">
    <h3>The form has <%= pluralize(resource.errors.count, "error") %></h3>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>